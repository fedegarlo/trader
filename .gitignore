# NUNCA subir extractos sin cifrar (solo se permiten los .csv.enc)
players/**/*.csv
*.csv.dec

# lista de extractos sin descifrar que genera `trader ranking` para la CI
pending.json

# caché de titulares: son de hoy y solo sirve de red de seguridad dentro del
# mismo build; versionarla solo publicaría noticias viejas
data/news/*.json

__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
